enum PinAction { number, scan, delete }

enum AppButtonIconPosition { left, right }

enum Gender { male, female, other, none }

enum RectangularRectangleTilePosition { left, center }

enum PasswordType { regular, confirm }

enum PaymentOption { card, bank }

enum AppBiometricType { fingerPrint, faceId }

enum LoadingState { normal, loading }

enum SelectionOrder { random, manual, none }

enum PinMode { set, reset }
